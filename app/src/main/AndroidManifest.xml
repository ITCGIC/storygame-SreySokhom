<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="gictitc.storygame.storygame">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="AllowBackup,GoogleAppIndexingWarning">
        <activity
            android:name=".activity.MainActivity"
            android:label="Menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.Ghost"
            android:label="Ghost Story" />
        <activity
            android:name=".activity.Love"
            android:label="Love Story" />
        <activity
            android:name=".activity.Sad"
            android:label="Sad Story" />
        <activity
            android:name=".ghost.Page1"
            android:label="Ghost Story"/>
        <activity
            android:name=".ghost.StayActivity"
            android:label="Ghost Story"/>
        <activity
            android:name=".ghost.GoOnActivity"
            >

        </activity>
    </application>

</manifest>